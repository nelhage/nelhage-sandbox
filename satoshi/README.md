# satoshi

Data analysis project.

## Setup

This project uses [uv](https://docs.astral.sh/uv/) for dependency management.

```sh
uv sync
```

To include the Jupyter kernel dependencies:

```sh
uv sync --group jupyter
```
